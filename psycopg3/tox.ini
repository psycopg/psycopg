[tox]
envlist = py{36,37,38}
isolated_build = True

[testenv:py{36,37,38}]
commands =
    pytest ../tests
passenv = PG* PSYCOPG3_TEST_DSN PYTEST_ADDOPTS PSYCOPG3_IMPL
deps =
    pytest >= 5.3, < 6
    pytest-asyncio >= 0.12.0, < 0.13
